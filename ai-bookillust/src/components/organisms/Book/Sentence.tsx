import { memo, VFC } from "react";

interface Props {
  sentence: string;
  withIllust: boolean;
  imagePath?: string;
  imageAlive?: boolean;
};

export function Sentence(props: Props) {
  return (
    <></>
  )
}
