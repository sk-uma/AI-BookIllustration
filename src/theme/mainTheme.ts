import { extendTheme } from '@chakra-ui/react';

const mainTheme = extendTheme({
    styles: {
        global: {
            body: {
                
            }
        }
    }
});

export default  mainTheme;
